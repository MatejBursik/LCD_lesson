Enter command prompt and clone this repo:

` git clone https://github.com/orangepi-xunlong/wiringOP `

Navigate into the WiringOP directory:

` cd wiringOP `

Clean any previous build artifacts:

` sudo ./build clean `

Build and install WiringOP:

` sudo ./build `
